prefix_rule(
    pattern = ["echo", "$SEARCH_QUERY"],
    decision = "allow",
    justification = "Allow reading the SEARCH_QUERY environment variable.",
)

prefix_rule(
    pattern = ["python3", "-c"],
    decision = "allow",
    justification = "Allow inline Python for reading and writing the news_flash_state.json state file.",
)

prefix_rule(
    pattern = [".claude/scripts/read_environment.py"],
    decision = "allow",
    justification = "Allow reading the Pachinko routing environment configuration as JSON.",
)
