{
  "name": "Sync arXiv",
  "version": "1.0.0",
  "supportedAgents": [
    "Claude Code",
    "Codex"
  ],
  "variables": {
    "FILTER_FILE": {
      "description": "An optional text file containing arXiv feed instructions. It may specify categories, search terms, authors, raw query fragments, a date range, relevance criteria, abstract-only or full-text notes, sort order, and a positive note limit or no limit. If not set or empty, all papers in the resolved window are eligible and the default 20-note limit applies.",
      "type": "file",
      "required": false
    }
  }
}
